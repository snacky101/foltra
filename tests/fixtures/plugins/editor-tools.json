{
  "kind": "plugin",
  "id": "editor-tools",
  "name": "편집 도구",
  "version": "1.0.0",
  "description": "선택한 글을 대문자로 바꾸거나 목록으로 정리하고, 현재 위치에 날짜를 삽입하세요. 일반 편집처럼 실행 취소할 수 있습니다.",
  "commands": [
    {
      "id": "uppercase",
      "title": "선택 영역 대문자로",
      "action": {
        "type": "script"
      }
    },
    {
      "id": "list",
      "title": "선택 영역을 목록으로",
      "action": {
        "type": "script"
      }
    },
    {
      "id": "date",
      "title": "현재 위치에 오늘 날짜 삽입",
      "action": {
        "type": "script"
      }
    }
  ],
  "runtime": {
    "apiVersion": 1,
    "permissions": [
      "editor.read",
      "editor.write"
    ],
    "settings": [
      {
        "id": "date-style",
        "label": "날짜 표시",
        "type": "select",
        "default": "YYYY-MM-DD",
        "options": [
          "YYYY-MM-DD",
          "YYYY/MM/DD"
        ]
      }
    ],
    "source": "// tests/fixtures/plugins/editor-tools/main.ts\nvar main_default = {\n  commands: {\n    uppercase(api) {\n      const selected = api.editor.read().selection;\n      if (selected) api.editor.replaceSelection(selected.toUpperCase());\n    },\n    list(api) {\n      const selected = api.editor.read().selection;\n      if (selected)\n        api.editor.replaceSelection(\n          selected.split(\"\\n\").map((line) => line.trim() ? `- ${line.replace(/^\\s*[-*+]\\s+/, \"\")}` : line).join(\"\\n\")\n        );\n    },\n    date(api) {\n      const now = /* @__PURE__ */ new Date();\n      const separator = api.settings[\"date-style\"] === \"YYYY/MM/DD\" ? \"/\" : \"-\";\n      api.editor.replaceSelection(\n        [\n          now.getFullYear(),\n          String(now.getMonth() + 1).padStart(2, \"0\"),\n          String(now.getDate()).padStart(2, \"0\")\n        ].join(separator)\n      );\n    }\n  }\n};\nexport {\n  main_default as default\n};\n"
  }
}
